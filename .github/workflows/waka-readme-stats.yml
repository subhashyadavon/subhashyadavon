name: wakatime-stats

on:
  push:
    branches: [main]
  schedule:
    # Runs every 6 hours
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme:
    name: Update README with WakaTime stats
    runs-on: ubuntu-latest
    # Skip runs triggered by this workflow's own commit (it pushes with a
    # personal GH_TOKEN, which would otherwise retrigger push indefinitely).
    if: ${{ !contains(github.event.head_commit.message, 'Updated with Dev Metrics') }}
    steps:
      - uses: actions/checkout@v7
        with:
          token: ${{ secrets.GH_TOKEN }}
      # This action has been flaky (hangs / transient failures), so retry up
      # to 3 times -- each attempt only runs if the previous one failed.
      - name: WakaTime stats (attempt 1)
        id: waka1
        continue-on-error: true
        uses: anmol098/waka-readme-stats@master
        with: &waka-inputs
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          SHOW_LINES_OF_CODE: "True"          # "From Hello World I've Written..." badge
          SHOW_TOTAL_CODE_TIME: "True"        # "Code Time" badge
          SHOW_SHORT_INFO: "True"             # 🐱 My GitHub Data block
          SHOW_COMMIT: "True"                 # Morning/Daytime/Evening/Night breakdown
          SHOW_TIMEZONE: "True"               # 🕑 Time Zone line
          SHOW_UPDATED_DATE: "True"           # "Last Updated on ..." footer
          SHOW_LANGUAGE: "True"               # 💬 Programming Languages (this week)
          SHOW_PROJECTS: "True"               # 🐱‍💻 Projects (this week)
          SHOW_DAYS_OF_WEEK: "True"           # Which days you're most productive
          # Left off: OS/editors are low-signal, and the chart (SHOW_LOC_CHART)
          # generates a PNG that needs to be committed on every run.
          SHOW_PROFILE_VIEWS: "False"
          SHOW_EDITORS: "False"
          SHOW_OS: "False"
          SHOW_LANGUAGE_PER_REPO: "False"
          SHOW_LOC_CHART: "False"

      - name: WakaTime stats (attempt 2)
        id: waka2
        if: steps.waka1.outcome == 'failure'
        continue-on-error: true
        uses: anmol098/waka-readme-stats@master
        with: *waka-inputs

      - name: WakaTime stats (attempt 3)
        if: steps.waka2.outcome == 'failure'
        uses: anmol098/waka-readme-stats@master
        with: *waka-inputs
